Olá <%= @invitation.email %>,

O usuário <%= @invitation.user.username %> te convidou para o <%= Rails.application.name %> (<%= Rails.application.root_url %>)<%= @invitation.memo.present? ? raw("\n\n   #{@invitation.memo}") : "" %>

Para aceitar esse convite e criar uma conta, visite a URL abaixo:

<%= Rails.application.root_url %>invitations/<%= @invitation.code %>
